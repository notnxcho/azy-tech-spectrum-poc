const QuestionCardContainer = ({questions, currentQuestions, setCurrentQuestions, focusedLayer}) => {
    return (
        <div className={`question-card-container ${focusedLayer !== 'chapter' ? 'display-card' : 'hide-card'}`}>
            holis
        </div>
    )
} 

export default QuestionCardContainer